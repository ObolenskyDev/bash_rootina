# 1. Мусор операционной системы (ты на Windows, это важно)
Thumbs.db
Desktop.ini
.DS_Store

# 2. Логи и временные файлы (папка monitoring и другие)
*.log
*.tmp
*.pid

# 3. Архивы и бэкапы (папка backups)
# Мы игнорируем тяжелые файлы, чтобы не засорять Гит
*.zip
*.tar
*.tar.gz
*.7z
*.bak
*.swp

# 4. Игнорируем ВСЁ содержимое папки backups, но саму папку оставляем
# (если хочешь, чтобы структура папок сохранилась)
backups/*
!backups/.gitkeep

# 5. Секреты (на всякий случай)
.env
config.private
id_rsa