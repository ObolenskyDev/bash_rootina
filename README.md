# Bash Rootina 🛠️

![Bash](https://img.shields.io/badge/Shell_Script-Bash-4EAA25?style=for-the-badge&logo=gnu-bash&logoColor=white)
![Linux](https://img.shields.io/badge/OS-Linux-FCC624?style=for-the-badge&logo=linux&logoColor=black)

**Bash Rootina** — это коллекция базовых скриптов автоматизации для задач системного администрирования и QA. 
Проект автоматизирует "рутинную" работу, обеспечивая стабильность инфраструктуры и возможность быстрой диагностики.

## 📂 Структура репозитория

### 1. Мониторинг (`/monitoring`)
**`health_check.sh`**
- **Назначение:** Проверка доступности API эндпоинтов.
- **Use Case:** Smoke-тестирование после деплоя для подтверждения статуса `200 OK`.
- **Логика:** Итеративная проверка списка URL-адресов сервисов с использованием `curl`.

### 2. Обслуживание (`/maintenance`)
**`clear_logs.sh`**
- **Назначение:** Освобождение дискового пространства путем удаления старых логов.
- **Use Case:** Запуск по расписанию (Cron) для удаления файлов логов старше 7 дней.
- **Логика:** Безопасная очистка с использованием команды `find` и флага `-mtime`.

### 3. Бэкапы (`/backups`)
**`backup_configs.sh`**
- **Назначение:** Резервное копирование конфигурационных файлов перед внесением изменений.
- **Use Case:** Стратегия быстрого отката (rollback) для конфигов Nginx или баз данных.
- **Логика:** Создание `.tar.gz` архивов с временной меткой в названии.

## 🚀 Инструкция по запуску

### **Клонирование репозитория:**
   ```bash
   git clone [https://github.com/ObolenskyDev/bash_rootina.git](https://github.com/ObolenskyDev/bash_rootina.git)
   ```

### Сделать скрипты исполняемыми::
```bash
chmod +x monitoring/health_check.sh
chmod +x maintenance/clear_logs.sh
```

### Запустить скрипт:
```bash
./monitoring/health_check.sh
```

---
